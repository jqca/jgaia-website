[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm && pnpm install"]

[phases.build]
cmds = ["pnpm --filter @workspace/jgaia run build"]

[start]
cmd = "npx serve artifacts/jgaia/dist -l $PORT"
